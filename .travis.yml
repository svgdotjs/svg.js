language: node_js
node_js:
    - "node"
script: npm test
sudo: true
before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y chrome
    - export DISPLAY=:99.0
    - sh -e /etc/init.d/xvfb start
